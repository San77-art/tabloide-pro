import React from 'react';
import ReactDOM from 'react-dom/client';
import TabloidePublico from './TabloidePublico';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <TabloidePublico />
  </React.StrictMode>,
);
