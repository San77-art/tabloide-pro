MP_ACCESS_TOKEN=
# Assinatura secreta do webhook, gerada em Suas integrações > Webhooks no painel do Mercado Pago.
# Usada para validar o header x-signature em /webhook/mp.
MP_WEBHOOK_SECRET=
FIREBASE_PROJECT_ID=tab-pro-b6c19
GOOGLE_APPLICATION_CREDENTIALS=./service-account.json

# No Railway não dá pra commitar o service-account.json (veja .gitignore) — cole o
# conteúdo inteiro do JSON aqui (em uma linha) e o backend usa isso em vez do arquivo.
FIREBASE_SERVICE_ACCOUNT_JSON=
